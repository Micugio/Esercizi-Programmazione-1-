#include <iostream>
using namespace std;

int main() {
    int n = 0;
    int flag = 9;

    do {
        cin >> n;
    } while (n<0)
    
    while (n>=0 && n<=flag) {
        if (n>=0 && n<=flag) {

        }
        flag = flag+9;
    }
    cifre = 

    cin >> n;
    int n = -1;
    int f0 = 0;
    int f1 = 1;

    int temp = 0;

    do {
        if (n<0)
    }
    cout << "Quanti numeri vuoi contare?" << endl;
    cin >> fib;
    

    if (fib==1) {
        cout << a << endl;
    }

    else if (fib==2) {
        flag = a+1
        cout << flag << endl;
    }

    else {
        while (i!=fib) {
        a = fib;
        b = (fib-1)+(a-2);
        i++;
        }
    }
    return 0;
}